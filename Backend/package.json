{
  "name": "farming-simulator-backend",
  "version": "1.0.0",
  "description": "Backend API for Farming Simulator with N-tier architecture",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "dev": "nodemon server.js",
    "test": "echo \"Error: no test specified\" && exit 1",
    "init-db": "node -e \"require('./src/init/dbInitialization')().then(() => process.exit(0)).catch(console.error)\""
  },
  "keywords": [
    "farming",
    "simulator",
    "api",
    "nodejs",
    "express",
    "mongodb"
  ],
  "author": "Farming Simulator Team",
  "license": "MIT",
  "dependencies": {
    "express": "^4.18.2",
    "mongoose": "^7.5.0",
    "cors": "^2.8.5",
    "dotenv": "^16.3.1"
  },
  "devDependencies": {
    "nodemon": "^3.0.1"
  },
  "engines": {
    "node": ">=16.0.0"
  }
}
